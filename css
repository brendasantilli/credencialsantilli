/* ==============================================================
   1. RESET Y VARIABLES BÁSICAS
   ============================================================== */
* {
    margin: 0;
    padding: 0;
    /* box-sizing fundamental para un buen manejo del modelo de caja */
    box-sizing: border-box; 
}

:root {
    /* Paleta de colores (podés cambiarlos a tu gusto) */
    --color-fondo-pantalla: #1a1a1d;
    --color-fondo-tarjeta: #ffffff;
    --color-texto-principal: #333333;
    --color-texto-secundario: #666666;
    --color-acento: #e30b5c; /* Color para botón y borde de imagen */
    --color-acento-hover: #b00847; /* Color para el estado hover */
}

/* ==============================================================
   2. ESTILOS GENERALES (Body)
   ============================================================== */
body {
    background-color: var(--color-fondo-pantalla);
    font-family: 'Helvetica Neue', Arial, sans-serif; /* Tipografía limpia */
    color: var(--color-texto-principal);
    
    /* Centrado perfecto de la credencial en la pantalla (sin scroll) */
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 20px; /* Margen de seguridad en pantallas chicas */
}

/* ==============================================================
   3. MODELO DE CAJA: LA CREDENCIAL / AFICHE
   ============================================================== */
.credencial {
    background-color: var(--color-fondo-tarjeta);
    width: 100%;
    max-width: 400px; /* Tamaño máximo para escritorio */
    padding: 40px; /* Espaciado interno (padding) */
    border-radius: 16px; /* Bordes redondeados */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4); /* Contraste y jerarquía visual */
    text-align: center;
}

/* ==============================================================
   4. COMPOSICIÓN Y ELEMENTOS INTERNOS
   ============================================================== */
.avatar {
    width: 140px;
    height: 140px;
    border-radius: 50%; /* Imagen circular */
    object-fit: cover;
    border: 4px solid var(--color-acento); /* Uso del borde (border) */
    margin-bottom: 20px; /* Margen externo (margin) */
}

.credencial-header h1 {
    font-size: 1.8rem;
    margin-bottom: 20px;
    color: var(--color-texto-principal);
}

.credencial-info p {
    font-size: 1rem;
    color: var(--color-texto-secundario);
    line-height: 1.5; /* Mejora la legibilidad */
    margin-bottom: 20px;
}

.credencial-info ul {
    list-style: none; /* Quita los puntos por defecto */
    background-color: #f4f4f5;
    padding: 15px;
    border-radius: 8px;
    margin-bottom: 25px;
    text-align: left; /* Lista alineada a la izquierda para leer fácil */
}

.credencial-info li {
    margin-bottom: 10px;
    font-size: 0.95rem;
}

.credencial-info li:last-child {
    margin-bottom: 0;
}

/* ==============================================================
   5. INTERACCIÓN (Estado :hover solicitado)
   ============================================================== */
.btn-enlace {
    display: inline-block;
    background-color: var(--color-acento);
    color: #ffffff;
    text-decoration: none;
    padding: 12px 28px;
    border-radius: 30px;
    font-weight: bold;
    /* Transición suave para el cambio de color */
    transition: background-color 0.3s ease, transform 0.3s ease;
}

/* El requerimiento de "Al menos un efecto hover" */
.btn-enlace:hover {
    background-color: var(--color-acento-hover);
    transform: translateY(-3px); /* Se eleva levemente al pasar el mouse */
}

/* ==============================================================
   6. RESPONSIVE DESIGN (Media Queries)
   ============================================================== */
@media (max-width: 480px) {
    /* Ajustes para teléfonos móviles */
    .credencial {
        padding: 25px; /* Menos padding en móviles */
    }
    
    .avatar {
        width: 110px;
        height: 110px;
    }

    .credencial-header h1 {
        font-size: 1.5rem;
    }
}
